name: pw test

on:  
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  e2eTest:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - run: npm i
      - name: Install PW
        run: npx playwright install --with-deps
      - name: Run PW test
        run: npm run test
      
      # Сохраняем отчеты
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: test-results
          retention-days: 20
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results
          path: allure-results
          retention-days: 20
      
      # 🔄 ИСПРАВЛЕННЫЙ БЛОК ДЛЯ ALLURE:
      - name: Generate Allure Report
        if: always()
        run: |
          # Генерируем отчет в папку allure-report
          npx allure generate ./allure-results --clean -o ./allure-report
          # Создаем правильный index.html для корня
          echo '<!DOCTYPE html><html><head><meta charset="utf-8"><meta http-equiv="refresh" content="0; URL=./allure-report/index.html"></head><body>Redirecting to <a href="./allure-report/index.html">Allure Report</a></body></html>' > ./index.html
      
      # 🔄 ИСПРАВЛЕННЫЙ ДЕПЛОЙ:
      - name: Deploy report
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: .  # 🔴 ИЗМЕНИТЬ: деплоим КОРЕНЬ проекта
          force_orphan: true
        env:
          # 🔴 ДОБАВИТЬ эти переменные:
          TZ: UTC